Example projects
================

Each folder here represents a Python distribution. Different packaging
approaches are tried here. This is used as sources of data for the static
checker (e.g. import analyzer).
